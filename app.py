name = "Randy"

#use the length function: len
if len(name) < 3:
    print ("must be at least 3 characters")
elif len(name) > 50:
    print ("only maximum of 50 characters")
else:
    print ("name looks good")


